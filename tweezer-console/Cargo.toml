[package]
name = "tweezer-console"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
tokio = { version = "1", features = ["io-util", "io-std", "rt-multi-thread", "sync"] }
tweezer = { path = "../tweezer" }
